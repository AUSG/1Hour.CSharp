// This is Top level statements as known as main function

Console.Write("Equivalent to print");
Console.WriteLine("Equivalent to println");

var a = 100; // Infer type as int. Recommend to use this.
const int constVal = 100; // Can't use var symbol for this time.
var arguments = args; // Program arguments. But this is not a global variable. Only available on top level statement.

return 0; // exit value.